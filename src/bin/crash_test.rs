fn write(){
    //so i want to be able to write 100 keys, automatic and random, need to build a secondary storage to compare if rebuilt hashmap matches
    7
}

fn verify(){
    Storage storage = Storage.new()
    //want to verify this somehow
}